#!/usr/bin/expect -f

set timeout -1

spawn ./mach bootstrap

expect "choice:"

send -- "2\r"

# FIXME -- create this directory, make sure it goes the same cached and uncached.

# expect "create this directory? (Yn):"

# send -- "\r"

expect "configured? (Yn):"

send -- "n\r"

expect "system telemetry? (Yn):"

send -- "y\r"

expect eof
